# 上线前替换为足够长的随机值，不要提交真实值。
ADMIN_PASSWORD=replace-with-a-strong-password
APP_SECRET=replace-with-at-least-32-random-characters

# 通过 HTTPS 反向代理访问时设为 true。
COOKIE_SECURE=true

HOST=0.0.0.0
PORT=8765
DATA_DIR=/app/data
